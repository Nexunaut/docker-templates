version: "2.1"
services:
  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - portainer_data/sonarr:/config
      - portainer_data/sonarr:/tv
      - portainer_data/sonarr:/downloads
    ports:
      - 8989:8989
    restart: unless-stopped

volumes:
  portainer_data:
    name: truenasData
    external: true
