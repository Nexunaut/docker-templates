version: "2.1"
services:
  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=TZ=America/Los_Angeles
    volumes:
      - truenasData/sonarr:/config
      - truenasData/tvseries:/tv
      - truenasData/downloadclient-downloads:/downloads
    ports:
      - 8989:8989
    restart: unless-stopped

volumes:
  truenasData:
    name: truenasData
    external: true
